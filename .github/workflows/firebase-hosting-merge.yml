# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build    
      - name: Echo Firebase Config
        run: |
          echo "Debugging Firebase Config"
          echo "${{ secrets.FIREBASE_CONFIG }}" | base64 --decode || true

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_VIDEO_892C6 }}
          channelId: live
          projectId: video-892c6
          firebaseConfig: ${{ secrets.FIREBASE_CONFIG }}
